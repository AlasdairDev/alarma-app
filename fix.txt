Act as an Elite Principal DevOps, Software Architect, and Senior .NET MAUI Engineer.

# 1. OPERATION: ARCHITECTURAL HYGIENE, AUTOMATED REPAIR, & OPTIMIZATION AUDIT
- PURPOSE: Perform a full-scale audit of the workspace to clean up the `AlarmaApp` file tree, enforce a strict Model-View-Controller (MVC) architecture, integrate the programmatic SVG Leaflet map marker patch, and deploy an autonomous self-repair utility.
- CRITICAL OBJECTIVE: You must retain 100% of existing application business logic and functions. Absolutely zero features or configuration dependencies can be dropped or broken during this consolidation pass.
- THE ZERO-TOLERANCE 1-TO-1 VISUAL RULE: Every single user interface element, border container, hex color definition, typographic weight, margin, and padding constraint MUST strictly adhere to the "GUI FROM FIGMA" design system metrics. You are completely prohibited from introducing structural changes that drift away from the approved design ecosystem.

# 2. STRICT SYSTEM CODE FREEZE (QUARANTINE ENFORCEMENT)
- DO NOT TOUCH THE TUTORIAL & EMERGENCY MODULES:
  1. You are STRICTLY FORBIDDEN from modifying, refactoring, opening, or changing `OnboardingView.xaml` or `OnboardingView.xaml.cs`.
  2. You are STRICTLY FORBIDDEN from modifying, refactoring, opening, or changing `EmergencyView.xaml` or `EmergencyView.xaml.cs`.
  - Both of these domains are locked down in a finalized, production-ready state to protect their working touch listeners, secure timers, direct cellular SMS tracking systems, and local SQLite data boundaries.

# 3. WORKSPACE TRANSFORMATION & MAINTENANCE TASKS

## A. REORGANIZING TO A STRICT MVC PATTERN
- Clean the workspace folder layout by mapping all components directly into clear architectural namespaces without altering existing functional dependencies or class behaviors:
    - `Controllers/`: Must cleanly enclose `HomeController.cs` containing all transactional business logic, `INotifyPropertyChanged` notifications, and input command operations.
    - `Models/`: Must strictly isolate data schemas and object contracts (`SavedRoute.cs`, `EmergencyContact.cs`, `TripHistory.cs`, etc.).
    - `Views/`: Must strictly isolate layout layers (`LaunchView`, `HomeView`, `SearchView`, `HistoryView`, `SettingsView`, and `AlarmStageView`).
    - `Services/`: Must retain local platform utility engines (SQLCipher local database engines, AES-GCM backup/restore managers, and dual-source geocoders).

## B. CONSOLIDATING GRAPHICS & LOCKING THE MAP PIN FIX
- KEEP CAPTURED ARTIFACTS: Do NOT delete the `GUI FROM FIGMA` folder under any circumstances; it must remain in the project root as your baseline design reference.
- FIX CODES PRESERVATION: Ensure your map-rendering routine inside the `MapWebView` HTML template uses the inline, base64-encoded purple SVG path string for the `L.icon` mapping definition. This completely removes external asset dependencies and locks down the pixel-perfect purple map locator pin, permanently preventing the standard green leaf asset fallback.
- ASSET PIPELINE HYGIENE: Verify all other user interface layout icons and background assets are safely duplicated into `AlarmaApp/Resources/Images/` and linked via uniform resource identifiers (e.g., `Source="main_logo.png"`).

## C. DEPLOYING THE WORKSPACE SELF-REPAIR UTILITY TOOL
- Create a lightweight automation tool (e.g., `repair_build.sh` or `repair_build.ps1`) in the solution root directory. The script must execute the following operations sequentially when fired:
    1. **Cache Purge**: Hard-delete all local `bin/` and `obj/` subdirectories throughout the solution workspace to eliminate stale, corrupt compilation artifacts.
    2. **Workload Alignment**: Trigger `dotnet workload restore` to assert the target `maui-android` compilation ecosystem is healthy.
    3. **Dependency Integrity Verification**: Execute `dotnet restore` to reconstruct the project package tree and enforce SQLCipher/SQLite cryptographic layer bindings.
    4. **Compilation Output validation**: Execute a clean build targeting Android 15 (API 35) to guarantee compilation stability.

# 4. ARCHITECTURAL OPTIMIZATION PERFORMANCE AUDIT
- Analyze the codebase and implement production-safe performance or security optimizations **strictly inside approved boundaries without changing any user functionality, existing features, or visual behaviors**. Focus exclusively on:
    - **WebView Render Acceleration**: Optimize the local caching policy and viewport layout layer configuration for the Leaflet.js `WebView` canvas inside `HomeView.xaml` to improve frame rates on lower-end devices.
    - **Debounce Efficiency Optimization**: Tighten memory allocation and prevent thread thrashing within the 500ms search input debounce thread pooling handler.
    - **Data virtualization**: Optimize query execution inside the SQLite data provider to accelerate list indexing when inflating records onto the `HistoryView` page.

# FINAL DIRECTIVE
- Run your new self-repair tool to clean the project structure, compile the codebase, and verify runtime execution.
- Report back with a brief summary detailing the cleaned MVC folder tree, the operational status of the custom map marker pin, and the newly applied stability improvements.
- Your output must explicitly confirm in a code-free summary: "Workspace folder structure cleaned and aligned to strict MVC design patterns, figma graphics verified, permanent SVG Leaflet pin fix locked down, self-repair utility script created, safe internal pipeline optimizations applied, and both OnboardingView and EmergencyView modules left completely untouched."